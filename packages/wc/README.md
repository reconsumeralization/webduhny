# `@webiny/wc`

This package contains the Webiny compiler (WC). It is a tool that compiles the Webiny manifest (`webiny.config.ts`) file into code and prepares the Webiny project for deployment and development.

> [!NOTE]
> This package is included in every Webiny wc by default, and it's not meant to be used as a standalone package.
