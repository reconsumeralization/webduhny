import React from "react";

interface FunnelEmbedProps {
    children: React.ReactNode;
}

const FunnelEmbed = ({ children }: FunnelEmbedProps) => {
    return <>{children}</>;
};

export default FunnelEmbed;
